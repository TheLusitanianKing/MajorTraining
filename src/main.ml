open Base
open Stdio
(* open Generation
open Model *)

let () =
  (* let steps = [] in
  let nb_rounds = 3 in
  let generated_circuit = generate steps all_exercises nb_rounds in *)

  Out_channel.output_string stdout @@ "Nothing yet.\n"